// Ajoute les matchers Jest supplémentaires pour tester le DOM
// (ex: toBeInTheDocument()). Chargé automatiquement par react-scripts
// avant chaque suite de tests.
import '@testing-library/jest-dom';
